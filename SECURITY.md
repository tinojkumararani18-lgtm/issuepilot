# Security

Do not commit GitHub tokens, webhook secrets, or API keys.

For deployment:
- use HTTPS;
- rotate secrets;
- restrict CORS;
- add authentication and rate limiting;
- validate webhook signatures;
- keep dependencies patched.
